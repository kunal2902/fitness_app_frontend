import 'package:flutter/material.dart';

import '../tabs/placeholder_tab.dart';

/// One-to-one video consults and subscription-based coaching.
///
/// Placeholder for now — see `PlaceholderTab`.
class AssistanceScreen extends StatelessWidget {
  const AssistanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderTab(title: 'Assistance');
  }
}
