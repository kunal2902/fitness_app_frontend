<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY. This file lives in src/debug, so it is never merged
  into a release APK and production traffic stays HTTPS-only.

  Android 9 (API 28) and later block plain HTTP by default. Development
  talks to http://10.0.2.2:3000, so cleartext is permitted for the loopback
  hosts below and nothing else.

  On a physical device, add your machine's LAN IP as another <domain>
  entry (for example 192.168.1.42) and point the app at it with the
  API_BASE_URL dart-define described in the README.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- How the Android emulator reaches the host machine's localhost. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
